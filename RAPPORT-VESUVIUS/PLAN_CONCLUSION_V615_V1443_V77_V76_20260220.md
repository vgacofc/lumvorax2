# PLAN DE CONCLUSION — V61.5, V144.3, V7.7, V7.6

Date: 2026-02-20

## Objectif
Lister précisément les phases/étapes restantes à conclure et définir le nouveau plan de convergence vers de meilleurs scores réels.

---

## 1) V61.5 — Phases restantes

## P1. Migration forensic runtime
- Intégrer bootstrap dépendances unifié (dataset V3 en priorité).
- Ajouter logs JSON signés + Merkle (niveau V144.2).
- Corriger définitivement le bug notebook `false`/`False`.

## P2. LUM integration
- Ajouter roundtrip `.lum` obligatoire sur volume test.
- Export diagnostics densité (`global`, `z-wise`, percentiles).

## P3. Calibration score
- Multi-seuil low/mid/high + contrainte densité cible.
- A/B/C avec garde-fou sur faux positifs.

## P4. GO criteria
- Submission conforme + run sans crash notebook.
- Densité dans plage cible validée + score > baseline V61.4.

---

## 2) V144.3 — Phases restantes

## P1. Durcissement anti-OOM
- OOM guard stage-wise (RAM budget + downgrade auto patch/batch).
- Profiler mémoire obligatoire dans forensic bundle.

## P2. Native bridge evidence
- Journaliser explicitement activation/désactivation bridge natif `.so`.
- KPI `% natif` dans `GLOBAL_STATS`.

## P3. Réduction du gap couverture train
- améliorer `train_pair_coverage_pct` (actuellement faible dans artefacts v144.2).
- augmenter couverture effective sans casser runtime.

## P4. Score convergence
- Stabiliser F-beta/F1 proxy + calibrage seuil final.
- objectifs: score > V144.2 et robustesse mémoire.

---

## 3) V7.7 — Phases restantes

## P1. Héritage V7.6 + forensic 360 complet
- conserver conformité Kaggle déjà acquise en V7.6.
- ajouter forensic JSON signé type V144.2 (pas seulement logs texte).

## P2. Multi-seuil + densité pilotée
- intégrer cascade seuils et auto-tuning densité.
- monitorer rappel/précision par tranche z.

## P3. Intégration `.lum`
- pipeline intermédiaire `.lum` optionnel activable.
- fallback Python documenté et tracé.

## P4. Score objective
- dépasser score réel V7.6 (0.303) avec conformité inchangée.

---

## 4) V7.6 — Phases restantes (version déjà exécutée)

## P1. Consolidation
- migrer logs forensic texte -> bundle JSON signé standard.
- ajouter preuve explicite dépendance résolue (manifest wheel + hash).

## P2. Optimisation score
- réduire sous-densité observée (~2.34%).
- ajuster `threshold_quantile`, `score_blend_3d_weight`, `z_smoothing_radius`.

## P3. Validation competition-grade
- vérifier reproductibilité sur plusieurs runs avec mêmes seeds.
- valider delta score positif stable.

---

## 5) Nouveau plan de convergence score (transverse)

## Axe A — Qualité signal
1. Ajustement densité cible par version (plages contrôlées).
2. Calibration seuil dynamique par z-slice.
3. Contrôle bruit/artifacts via morphologie adaptative.

## Axe B — Robustesse système
1. Bootstrap dépendances strict unifié.
2. Forensic runtime commun (events obligatoires).
3. Anti-OOM standard + checkpoints.

## Axe C — Validation scientifique
1. Matrice d’expériences standardisée (A/B/C/D) par version.
2. Tableau comparatif des métriques techniques + score LB.
3. Règles GO/NO-GO identiques sur toutes branches.

---

## 6) Questions critiques à trancher avant implémentation finale

1. Quelle plage densité cible officielle par famille de version (V61.x/V7.x/V144.x)?
2. Le bridge natif `.so` devient-il critère bloquant de release?
3. Quelle limite RAM hard pour éviter le scénario `Notebook Out of Memory`?
4. Quel budget temps max de run par version (Kaggle wall-time)?
5. Quelle stratégie de rollback si score baisse malgré conformité technique?

