Welcome to fish, the friendly interactive shell
Type help for instructions on how to use fish
lvx@lvx-Vostro-5481 ~> # ═══ TERMINAL 2 Ubuntu — Run BTC C47 Illimité ═══

                       cd ~/LVX/lumvorax2/src/advanced_calculations/bitcoin_quantum_mining

                       # 1. Compiler avec les instructions natives du CPU Ubuntu
                                                                              tu
                       make clean && make all 2>&1 | tail -5                  tu
                                                                              tu
                       # 2. Vérifier CPUs + lancer le run illimité
                       NPROC=$(nproc)                                         tu
                       STAMP=$(date -u +%Y%m%dT%H%M%SZ)
                       RUN_ID="btc_${STAMP}_c47_ubuntu"                       tu
                       echo "CPUs: $NPROC | Run: $RUN_ID"
                                                                              tu
                       REAL_HEADER=$(doppler run -- python3 scripts/fetch_btc_real_pow.py 2>/dev/null | grep '^[0-9a-f]' | head -1)
                       echo "Header: ${REAL_HEADER:0:20}... (${#REAL_HEADER} cturs)"                   # ═══ TERMINAL 2 Ubuntu — Run BTC C47 Illimité ═══

                       cd ~/LVX/lumvorax2/src/advanced_calculations/bitcoin_quantum_mining                   --threads $NPROC \
                             --duration-s 0 \
                       # 1. Compiler avec les instructions natives du CPU Ubuntu

                       make clean && make all 2>&1 | tail -5
                                                \
                       # 2. Vérifier CPUs + lancer le run illimité
                       NPROC=$(nproc)
                       STAMP=$(date -u +%Y%m%dT%H%M%SZ)
                       RUN_ID="btc_${STAMP}_c47_ubuntu"x "$REAL_HEADER"} 
                       echo "CPUs: $NPROC | Run: $RUN_ID"
                                                                         \
                       REAL_HEADER=$(doppler run -- python3 scripts/fetch_btc_real_pow.py 2>/dev/null | grep '^[0-9a-f]' | head -1)
                       echo "Header: ${REAL_HEADER:0:20}... (${#REAL_HEADER} chars)"

                       doppler run -- ./btc_mining_runner \g
                             --threads $NPROC \
                             --duration-s 0 \
                             --mode UBUNTU_C47_UNLIMITED \
                             --run-id "$RUN_ID" \
                             ${REAL_HEADER:+--header-hex "$REAL_HEADER"} \
                             2>&1 | tee ~/btc_c47_ubuntu.log



fish: Unsupported use of '='. In fish, please use 'set NPROC $(nproc)'.
NPROC=$(nproc)
^~~~~~~~~~~~~^
lvx@lvx-Vostro-5481 ~> # ═══ TERMINAL 2 Ubuntu — Run BTC C47 Illimité ═══

                       cd ~/LVX/lumvorax2/src/advanced_calculations/bitcoin_quantum_mining

                       # 1. Compiler avec les instructions natives du CPU Ubuntu
lvx@lvx-Vostro-5481 ~> # ═══ TERMINAL 2 Ubuntu — Run BTC C47 Illimité ═══

                       cd ~/LVX/lumvorax2/src/advanced_calculations/bitcoin_quantum_mining

                       # 1. Compiler avec les instructions natives du CPU Ubuntu

                       make clean && make all 2>&1 | tail -5

                       # 2. Vérifier CPUs + lancer le run illimité
                       NPROC=$(nproc)
                       STAMP=$(date -u +%Y%m%dT%H%M%SZ)
                       RUN_ID="btc_${STAMP}_c47_ubuntu"
                       echo "CPUs: $NPROC | Run: $RUN_ID"

                       REAL_HEADER=$(doppler run -- python3 scripts/fetch_btc_real_pow.py 2>/dev/null | grep '^[0-9a-f]' | head -1)
                       echo "Header: ${REAL_HEADER:0:20}... (${#REAL_HEADER} chars)"

                       doppler run -- ./btc_mining_runner \
                             --threads $NPROC \
                             --duration-s 0 \
                             --mode UBUNTU_C47_UNLIMITED \
                             --run-id "$RUN_ID" \
                             ${REAL_HEADER:+--header-hex "$REAL_HEADER"} \
                             2>&1 | tee ~/btc_c47_ubuntu.log


                                                            ^C
lvx@lvx-Vostro-5481 ~> cd ./LVX/lumvorax2
lvx@lvx-Vostro-5481 ~/L/lumvorax2 (main)> # ═══ TERMINAL 2 Ubuntu — Run BTC C47 Illimité ═══

                                          cd ~/LVX/lumvorax2/src/advanced_calculations/bitcoin_quantum_mining

                                          # 1. Compiler avec les instructions natives du CPU Ubuntu
                                          make clean && make all 2>&1 | tail -5

                                          # 2. Vérifier CPUs + lancer le run illimité
                                          NPROC=$(nproc)
                                          STAMP=$(date -u +%Y%m%dT%H%M%SZ)
                                          RUN_ID="btc_${STAMP}_c47_ubuntu"
                                          echo "CPUs: $NPROC | Run: $RUN_ID"

                                          REAL_HEADER=$(doppler run -- python3 scripts/fetch_btc_real_pow.py 2>/dev/null | grep '^[0-9a-f]' | head -1)
                                          echo "Header: ${REAL_HEADER:0:20}... (${#REAL_HEADER} chars)"

                                          doppler run -- ./btc_mining_runner \
                                                --threads $NPROC \

                                                --duration-s 0 
                                                               \



                                                --mode UBUNTU_C47_UNLIMITED 

                                                                            \





                                                --run-id "$RUN_ID" 


                                                                   \







                                                ${REAL_HEADER:+--header-hex "$REAL_HEADER"}                               # ═══ TERMINAL 2 Ubuntu — Run BTC C47 Illimité ═══

                                          cd ~/LVX/lumvorax2/src/advanced_calculations/bitcoin_quantum_mining
            \
                                          # 1. Compiler avec les instructions natives du CPU Ubuntu
                                          make clean && make all 2>&1 | tail -5

                                          # 2. Vérifier CPUs + lancer le run illimité
                                          NPROC=$(nproc)
                                          STAMP=$(date -u +%Y%m%dT%H%M%SZ)
                                          RUN_ID="btc_${STAMP}_c47_ubuntu"
                                          echo "CPUs: $NPROC | Run: $RUN_ID"

                                          REAL_HEADER=$(doppler run -- python3 scripts/fetch_btc_real_pow.py 2>/dev/null | grep '^[0-9a-f]' | head -1)
                                          echo "Header: ${REAL_HEADER:0:20}... (${#REAL_HEADER} chars)"

                                          doppler run -- ./btc_mining_runner \
                                                --threads $NPROC \
                                                --duration-s 0 \
                                                --mode UBUNTU_C47_UNLIMITED \
                                                --run-id "$RUN_ID" \
                                                ${REAL_HEADER:+--header-hex "$REAL_HEADER"} \
                                                2>&1 | tee ~/btc_c47_ubuntu.log






fish: Unsupported use of '='. In fish, please use 'set NPROC $(nproc)'.
NPROC=$(nproc)
^~~~~~~~~~~~~^
lvx@lvx-Vostro-5481 ~/L/lumvorax2 (main)> # ═══ TERMINAL 2 Ubuntu — Run BTC C47 Illimité ═══

                                          cd ~/LVX/lumvorax2/src/advanced_calculations/bitcoin_quantum_mining

                                          # 1. Compiler avec les instructions natives du CPU Ubuntu
                                          make clean && make all 2>&1 | tail -5

                                          # 2. Vérifier CPUs + lancer le run illimité
lvx@lvx-Vostro-5481 ~/L/lumvorax2 (main)> # ═══ TERMINAL 2 Ubuntu — Run BTC C47 Illimité ═══

                                          cd ~/LVX/lumvorax2/src/advanced_calculations/bitcoin_quantum_mining

                                          # 1. Compiler avec les instructions natives du CPU Ubuntu
                                          make clean && make all 2>&1 | tail -5

                                          # 2. Vérifier CPUs + lancer le run illimité
                                          NPROC=$(nproc)
                                          STAMP=$(date -u +%Y%m%dT%H%M%SZ)
                                          RUN_ID="btc_${STAMP}_c47_ubuntu"
                                          echo "CPUs: $NPROC | Run: $RUN_ID"

                                          REAL_HEADER=$(doppler run -- python3 scripts/fetch_btc_real_pow.py 2>/dev/null | grep '^[0-9a-f]' | head -1)
                                          echo "Header: ${REAL_HEADER:0:20}... (${#REAL_HEADER} chars)"

                                          doppler run -- ./btc_mining_runner \
                                                --threads $NPROC \
                                                --duration-s 0 \
                                                --mode UBUNTU_C47_UNLIMITED \
                                                --run-id "$RUN_ID" \
                                                ${REAL_HEADER:+--header-hex "$REAL_HEADER"} \
                                                2>&1 | tee ~/btc_c47_ubuntu.log






