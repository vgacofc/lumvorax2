# Rapport d'Analyse Bob (Mode Fallback)

Erreur: Command failed: export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" && nvm use v22.22.3 && export $(cat /home/lvx/LVX/lumvorax2/src/MDBAI/.env.bob | xargs) && bob -y --chat-mode code --output-format json -p "Analyse ce code et génère un rapport JSON"
nvm is not compatible with the "npm_config_prefix" environment variable: currently set to "/usr/local"
Run `unset npm_config_prefix` to unset it.

