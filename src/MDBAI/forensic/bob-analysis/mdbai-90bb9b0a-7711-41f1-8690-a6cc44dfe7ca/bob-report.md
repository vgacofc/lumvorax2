# Rapport d'Analyse Bob (Mode Fallback)

Erreur: Command failed: export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" && nvm use v22.22.3 && bob -y --chat-mode code --output-format json -p "Analyse ce code et génère un rapport JSON"
YOLO mode is enabled. All tool calls will be automatically approved.
{
  "error": {
    "type": "Error",
    "message": "API key required. Set BOBSHELL_API_KEY via environment variable or .env file.",
    "code": 1
  }
}

