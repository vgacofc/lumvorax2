# LFS DESACTIVE — C65 — Rotation 50MB appliquee a tous les logs
# Le fichier btc_qm_engine_metrics.log est sorti du tracking LFS.
# Tous les logs forensic sont dans .gitignore. Aucun fichier > 50MB ne sera commite.
